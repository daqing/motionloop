// Package agent implements the motionloop agent runtime: the tool
// interface, the LLM-to-tool execution loop, its event stream, and a
// scripted fake provider for tests. It corresponds to pi-agent-core's loop
// fundamentals; hooks and parallel execution arrive in later phases.
package agent
